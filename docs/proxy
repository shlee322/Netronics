Proxy Service는 프론트엔드랑 직접적으로 통신하는 "Service"이며 방화벽 외부에
존재한다.

또한 클라이언트와 직접적으로 통신하는 부분인 만큼 P2P 통신에 없어서는 안될 중요한 부분이다.
P2P에 관한 자세한 내용은 p2p 문서를 참고 하길 바란다.

Proxy Service와 Client과의 통신은 BSON을 SSL 프로토콜 위에 얹어 사용한다.

기본적으로 사용되는 프로토콜은 다음과 같다.

Server->Client {type:"ping"}
Client->Server {type:"pong"}
